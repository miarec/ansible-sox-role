---
sox_package_dependencies: []

# sox_packages:
#   - libogg-dev
#   - libmad0
#   - lame
#   - sox
#   - libsox-fmt-base
#   - libsox-fmt-mp3

# sox_base_packages:
#   - sox

# sox_format_packages:
#   - libsox-fmt-base
#   - libsox-fmt-mp3

# sox_format_package:
#   - mp3: libsox-fmt-mp3
  # - ogg: libsox-fmt-base
  # - flac: libsox-fmt-base
  # -




#   https://packages.debian.org/search?keywords=libsox&searchon=names&suite=bookworm&section=all

# Ubuntu 22.04
# just from apt install sox
# ogg, wav and flac,   no mp3

#   AUDIO FILE FORMATS: 8svx aif aifc aiff aiffc al amb amr-nb amr-wb anb au avr awb caf cdda cdr cvs cvsd cvu dat dvms f32 f4 f64 f8 fap flac fssd gsm gsrt hcom htk ima ircam la lpc lpc10 lu mat mat4 mat5 maud nist ogg paf prc pvf raw s1 s16 s2 s24 s3 s32 s4 s8 sb sd2 sds sf sl sln smp snd sndfile sndr sndt sou sox sph sw txw u1 u16 u2 u24 u3 u32 u4 u8 ub ul uw vms voc vorbis vox w64 wav wavpcm wv wve xa xi

# apt install libsox-fmt-mp3

# mp3 now supported

# tested by converting files


# Ubuntu 20.04

# Same
# ogg, flac, wav  supported with base package

# root@instance:/# sox -n -r 16000 sample.wav synth 3 sine 500
# root@instance:/# sox -r 8000 sample.wav output.wav
# root@instance:/# sox -r 8000 sample.wav output.ogg
# root@instance:/# sox -r 8000 sample.wav output.flac
# root@instance:/# sox -r 8000 sample.wav output.mp3
# sox FAIL formats: no handler for file extension `mp3'
# root@instance:/#